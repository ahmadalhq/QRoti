<!DOCTYPE html>
<html>
    <head>
        <title>Search Menu</title>
        <link href="/style/supply.css" type="text/css" rel="stylesheet"/>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
        <link href="https://fonts.googleapis.com/css?family=Lato|Spartan" rel="stylesheet">
    </head>
    <body>
        <%- include('../partials/supplyheader.ejs')%>
        <div class="container-fluid">
            <form action="/supply/search" method="POST">
                <input type="text" placeholder="What item are you looking to?" name="name">
                <input type="submit">
            </form>
            <div class="table">
            <tr>
                <td>ID</td>
                <td>Name</td>
                <td>Type</td>
                <td>Quantity</td>
                <td>Price</td>
            </tr>
            <% for (let i = 0; i < data.length ; i++){ %>
            <tr>
                <td><%= data[i].id %></td>
                <td><%= data[i].name %></td>
                <td><%= data[i].type %></td>
                <td><%= data[i].quantity %></td>
                <td><%= data[i].price %></td>
            </tr>
            <% } %>
            </div>
        </div>
    </body>
    <%- include('../partials/footer.ejs')%>
</html>